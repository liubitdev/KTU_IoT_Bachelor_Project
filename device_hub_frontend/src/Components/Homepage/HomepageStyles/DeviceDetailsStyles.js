import styled from 'styled-components';
import { Box } from '@material-ui/core';

export const DeviceDetailsContainer = styled(Box)`
  padding: 0.5em 0;
`;

export const DeviceTextBold = styled.p`
  display: inline;
  font-weight: bold;
`;
