import styled from 'styled-components';
import { Box } from '@material-ui/core';

export const RuleListHeader = styled.h1`
  font-size: 1.5em;
`;

export const RuleContainer = styled(Box)`
  padding: 1em 0;
`;
